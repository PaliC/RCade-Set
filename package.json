{
  "name": "set",
  "private": false,
  "version": "2.0.0",
  "description": "Find three cards that form a \"set\" — for each feature (shape, color, number, shading), the three cards must be all the same or all different.",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite --port 5173 --strictPort\" \"rcade dev http://localhost:5173\"",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "rcade": "^0.0.38",
    "typescript": "^5.3.0",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@rcade/plugin-input-classic": "^0.2.1"
  }
}
